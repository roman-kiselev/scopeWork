/*
import { IUnitSlice } from './unitSlice'
import { Action, CaseReducer } from '@reduxjs/toolkit'

const test: CaseReducer = (state, action) => {

    return {

    }
}

interface IHandle<S, A extends Action<any>> {

    pending(): CaseReducer<S, A>;

    fulfilled: CaseReducer<S, A>;

    rejected: CaseReducer<S, A>;
}

interface ITest extends Action<any> {
    id: number
}

class HandleCreateUnit implements IHandle<IUnitSlice, ITest> {

}


export default new HandleCreateUnit()*/
export {}